@pragma once TEST_H

@define MY_PRINT = 
printf("hello\n");
@end_define 

generic_class std::Result<T> {
    T value;
    int err;
}; 

generic_class math::vec4<T> {
    repeat(4, T v$i;)
};
@msg r
allowapply std::Result<int> as intResult
@msg v
allowapply math::vec4<float> as vec4
class math::Point {
    float x;
    float y;
};

int add(int* self, int x) {
    return *self + x;
}

int main() {

    // comments
    /*
        block comments
    */

    math::Point p;

    int *ptr = new(int);
    *ptr = 5;
    int *ptr2 = new(int);
    *ptr2 = 1;
    task clean =  delete(ptr);
    task clean ++ delete(ptr2);

    int x = 5;
@msg x
    int y = x:add(10);
@msg x2
    $a= 999
    int z = $a;

    vec4 v;

    MY_PRINT

    const char* s = "repeat(999, nope)";
    const char* s2 = "class test { int x; }";

    task clean run

    return 0;
}